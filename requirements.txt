# ============================================================
# ADMM-Diff / BEVFormer 纯 Python 依赖（版本全部锁定）
# 说明：
#   - 此文件由 Dockerfile 在装好 torch 之后、装 mm 栈之前安装。
#   - torch / torchvision / mmcv-full / mmdet3d / detectron2 不在这里，
#     它们在 Dockerfile 里用对应 CUDA(cu111) 的渠道单独安装。
#   - numpy / numba / llvmlite 必须锁死，否则会被其它包升级导致 numba 崩溃。
# ============================================================

# ---------- 科学计算基座（必须锁死）----------
numpy==1.19.5
numba==0.48.0
llvmlite==0.31.0
scipy==1.10.1
pandas==1.4.4
scikit-image==0.19.3
scikit-learn==1.3.2
matplotlib==3.5.2
networkx==2.2
Pillow==9.5.0
opencv-python==4.12.0.88
seaborn==0.12.2

# ---------- 扩散模型相关 ----------
diffusers==0.17.1
transformers==4.25.1
tokenizers==0.13.3
huggingface-hub==0.16.4
accelerate==0.16.0
safetensors==0.3.1
datasets==2.13.0
ftfy==6.2.3
wandb==0.15.8

# ---------- 3D 检测 / nuScenes ----------
nuscenes-devkit==1.1.10
pyquaternion==0.9.9
Shapely==1.8.5
plyfile==1.0.3
trimesh==2.35.39
lyft-dataset-sdk==0.0.8
pycocotools==2.0.7
terminaltables==3.1.10

# ---------- DD3D / 通用工具 ----------
einops==0.8.1
fvcore==0.1.5.post20221221
iopath==0.1.9
timm==0.6.13
typing-extensions==4.5.0
tabulate==0.9.0
Jinja2==3.1.6
tensorboard==2.14.0
yapf==0.40.1
